/**
 * @providesModule TableView
 * @flow
 */

'use strict';

var {requireNativeComponent} = require('react-native');

var TableView = requireNativeComponent('TableView', null);

module.exports = TableView;
